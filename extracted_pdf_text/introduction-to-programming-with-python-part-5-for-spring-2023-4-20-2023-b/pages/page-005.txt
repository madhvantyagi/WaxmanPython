Problem:
Like the problem above, calculate and return the diagonal sum, but for the elements along the diagonal
going from the top right to the lower left.
List Comprehensions
A list comprehension is a very compact and useful notation that Python provides for creating lists.
It is very similar to the notation that is uses in math for specifying sets. For example it is pretty clear
what this means:
{x2 : 1≤ x ≤100 , if x is even}
It is the set of the squares of the even numbers between 1 and 100.
Here is a list comprehension that creates a list with the same numbers:
Notice that this is equivalent to the following:
a=[]
for x in range(1,101):
if x%2==0:
a.append(x)
The list comprehension notation is more compact and much clearer (once you get used to it) than the
equivalent code above
114
